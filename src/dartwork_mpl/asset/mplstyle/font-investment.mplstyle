font.family: sans-serif
font.sans-serif: Roboto, Lato, Inter, Open Sans, Arial, Helvetica, sans-serif

mathtext.fontset: custom  # Should be 'dejavusans' (default),

mathtext.bf:  Noto Sans Math:bold
mathtext.cal: Noto Sans Math
mathtext.it:  Noto Sans Math:italic
mathtext.rm:  Noto Sans Math
mathtext.sf:  Noto Sans Math
mathtext.tt:  Noto Sans Math
mathtext.fallback: None  # Select fallback font from ['cm' (Computer Modern), 'stix'
                        # 'stixsans'] when a symbol can not be found in one of the
                        # custom math fonts. Select 'None' to not perform fallback
                        # and replace the missing character by a dummy symbol.
mathtext.default: it  # The default font to use for math.
                       # Can be any of the LaTeX font names, including
                       # the special name "regular" for the same font
                       # used in regular text.

#font.style:   normal
#font.variant: normal
font.weight:  300
#font.stretch: normal
font.size:    8.0

axes.titlesize:     9.0   # font size of the axes title
axes.titleweight:   400  # font weight of title
axes.labelsize:     8.0  # font size of the x and y labels
axes.labelweight:   400  # weight of the x and y labels

xtick.labelsize:     7.5  # font size of the tick labels
ytick.labelsize:     7.5  # font size of the tick labels

legend.fontsize:     6.0

figure.titlesize:   9.0    # size of the figure title (``Figure.suptitle()``)
figure.titleweight: 400    # weight of the figure title
figure.labelsize:   8.0     # size of the figure label (``Figure.sup[x|y]label()``)
figure.labelweight: 300    # weight of the figure label
