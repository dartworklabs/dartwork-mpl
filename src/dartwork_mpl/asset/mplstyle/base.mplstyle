# font.family MUST be the explicit family list (not the generic
# `sans-serif` alias) for matplotlib's per-glyph fallback to work in
# PLAIN text: _find_fonts_by_props() builds its fallback chain from
# prop.get_family(), and a generic `sans-serif` resolves to a SINGLE
# best-scoring font — so every glyph the primary face lacks becomes tofu
# regardless of what font.sans-serif lists. Listing only the *bundled*
# families (+ the `sans-serif` terminator) keeps the fallback chain
# deterministic and silent. font.sans-serif is held IDENTICAL to
# font.family: only bundled families plus the generic `sans-serif`
# terminator. Non-bundled system fonts (Lato, Open Sans, Arial, Helvetica,
# Gothic A1, Freesentation, AppleGothic, Malgun Gothic) were removed —
# anything past the bundle intentionally falls to matplotlib's default
# rather than a machine-dependent OS font, so output is reproducible across
# machines. The bundled corpus already covers the scientific/report symbol
# set (see tests/test_font_glyph_coverage.py). Both chains are pinned by
# tests/test_style_chain_parity.py.
font.family: Roboto, Inter, Paperlogy, Noto Sans CJK KR, Pretendard, Noto Sans Math, Noto Sans Symbols, Noto Sans Symbols 2, sans-serif
font.sans-serif: Roboto, Inter, Paperlogy, Noto Sans CJK KR, Pretendard, Noto Sans Math, Noto Sans Symbols, Noto Sans Symbols 2, sans-serif
# Monospace role chain: JetBrains Mono (default) → D2Coding (mono-kr, for
# monospaced Hangul in code blocks and aligned Korean tables) → the Latin
# mono siblings, then the generic terminator. For Korean code/tables set
# font.family=["JetBrains Mono", "D2Coding"] so Hangul stays fixed-width.
font.monospace: JetBrains Mono, D2Coding, IBM Plex Mono, Roboto Mono, Source Code Pro, monospace
lines.linewidth: 1.0               # line width in points
lines.color:     oc.blue4                # has no affect on plot(); see axes.prop_cycle
lines.markersize: 3.0              # compact default marker size for report charts
lines.dashed_pattern: 3, 1.5
lines.dashdot_pattern: 1, 1.25, 4, 1.25
lines.dotted_pattern: 1.0, 1.25

patch.linewidth:       0.5    # edge width in points.

#font.style:   normal
#font.variant: normal
font.weight:  300
#font.stretch: normal
font.size:    7.5

# Custom mathtext fontset matched to the BODY family so a math segment
# ($R^2$, $\mu m$) renders in the same voice as the labels around it,
# instead of leaking matplotlib's DejaVu default. Every face below MUST
# resolve to a real font file (verified in tests/test_font_invariants.py):
# rm/it/bf/sf -> Roboto (the body face), tt -> JetBrains Mono (the mono
# role), cal -> STIXGeneral (matplotlib-bundled, the only calligraphic
# face we can resolve without shipping one). Greek/operators absent from
# these fall to `stixsans` (matplotlib-bundled, zero extra bytes) — NOT
# DejaVu. `mathtext.default: regular` makes bare mathtext use the same
# upright body face as regular text, so numbers and Latin in `$…$` match
# the surrounding labels.
mathtext.fontset: custom  # Should be 'dejavusans' (default),

mathtext.bf:  Roboto:bold
mathtext.cal: STIXGeneral:italic
mathtext.it:  Roboto:italic
mathtext.rm:  Roboto
mathtext.sf:  Roboto
mathtext.tt:  JetBrains Mono
mathtext.fallback: stixsans  # Select fallback font from ['cm' (Computer
                        # Modern), 'stix', 'stixsans'] when a symbol is not
                        # in one of the custom math faces above. 'stixsans'
                        # is bundled by matplotlib (zero extra bytes) and
                        # keeps the fallback sans-serif to match the body,
                        # resolving gracefully instead of a dummy box.
mathtext.default: regular  # The default font to use for math.
                       # Can be any of the LaTeX font names, including
                       # the special name "regular" for the same font
                       # used in regular text.

axes.linewidth:     0.3     # edge line width
axes.grid:          False   # display grid or not
axes.titlesize:     8.5   # font size of the axes title
axes.titleweight:   bold  # font weight of title
axes.labelsize:     7.5  # font size of the x and y labels
axes.labelweight:   400  # weight of the x and y labels
axes.axisbelow:     True    # draw axis gridlines and ticks:
                             #     - below patches (True)
                             #     - above patches but below lines ('line')
                             #     - above all (False)

#axes.spines.left:   True  # display axis spines
#axes.spines.bottom: True
axes.spines.top:    False
axes.spines.right:  False

axes.xmargin:   0.05  # x margin.  See `axes.Axes.margins`
axes.ymargin:   0.1   # y margin.  See `axes.Axes.margins`
axes.zmargin:   0.05  # z margin.  See `axes.Axes.margins`

xtick.major.size:    2     # major tick size in points
xtick.minor.size:    1       # minor tick size in points
xtick.major.width:   0.3     # major tick width in points
xtick.minor.width:   0.20     # minor tick width in points
xtick.major.pad:     2     # distance to major tick label in points
xtick.minor.pad:     2     # distance to the minor tick label in points
#xtick.color:         black   # color of the ticks
#xtick.labelcolor:    inherit # color of the tick labels or inherit from xtick.color
xtick.labelsize:     7  # font size of the tick labels
xtick.direction:     out     # direction: {in, out, inout}
xtick.minor.visible: False   # visibility of minor ticks on x-axis

ytick.major.size:    2     # major tick size in points
ytick.minor.size:    1       # minor tick size in points
ytick.major.width:   0.3     # major tick width in points
ytick.minor.width:   0.20     # minor tick width in points
ytick.major.pad:     1     # distance to major tick label in points
ytick.minor.pad:     1     # distance to the minor tick label in points
#ytick.color:         black   # color of the ticks
#ytick.labelcolor:    inherit # color of the tick labels or inherit from ytick.color
ytick.labelsize:     7  # font size of the tick labels
ytick.direction:     out     # direction: {in, out, inout}
ytick.minor.visible: False   # visibility of minor ticks on y-axis

grid.color:    oc.gray3        # grid color
grid.linestyle: -      # solid
grid.linewidth: 0.3         # in points

legend.frameon:       False     # if True, draw the legend on a background patch
legend.framealpha:    1.0      # legend patch transparency
legend.edgecolor:     0.0      # background patch boundary color
legend.fancybox:      False     # if True, use a rounded box for the
legend.numpoints:     1        # the number of marker points in the legend line
legend.markerscale:   0.5      # keep legend markers visually quiet
legend.fontsize:      6.5

## Dimensions as fraction of font size:
legend.borderpad:     0.1  # border whitespace
legend.labelspacing:  0.4  # the vertical space between the legend entries
legend.handletextpad: 0.3  # the space between the legend line and legend text
legend.borderaxespad: 0.4  # the border between the axes and legend edge
legend.columnspacing: 0.75  # column separation

figure.titleweight: 400    # weight of the figure title
figure.labelsize:   7.5     # size of the figure label (``Figure.sup[x|y]label()``)
figure.labelweight: 300    # weight of the figure label
figure.figsize:     3.5, 3.0  # figure size in inches
figure.dpi:         300       # figure dots per inch
figure.facecolor:   white     # figure face color

savefig.dpi:       300      # figure dots per inch or 'figure'
savefig.facecolor: white        # figure face color when saving
savefig.bbox:      standard    # {tight, standard}
ps.fonttype:       42       # Output Type 3 (Type3) or Type 42 (TrueType)

# v5 categorical cycle (spec §8) — 8 color-only entries. Kept color-only
# (NOT a linestyle product) because a linestyle in the DEFAULT prop_cycle is
# a footgun: any `ax.plot(..., lw=0)` (zero-width lines — common for
# invisible-edge constructs) that inherits a dashed/dotted linestyle from the
# cycle crashes matplotlib ("At least one value in the dash list must be
# positive" — dash pattern scaled by lw=0). For >8 line series needing extra
# distinguishability, opt in per-Axes with `dm.set_colors(ax=ax, styles=True)`,
# which adds the linestyle extension only where it is safe. The dc.* tokens
# resolve because dartwork_mpl registers its named colors into matplotlib's
# global mapping on package import, before Style.stack() parses this file.
axes.prop_cycle: cycler('color', ['dc.blue6', 'dc.orange9', 'dc.green5', 'dc.pink3', 'dc.amber7', 'dc.violet8', 'dc.cyan8', 'dc.rose8'])
image.cmap: dc.aurora
